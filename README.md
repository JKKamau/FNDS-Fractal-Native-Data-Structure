# FNDS-Fractal-Native-Data-Structure
By viewing data through the lens of "irregular topology," FNDS is an algorithm that compresses data by identifying the fractal dimension of a dataset rather than its bit-patterns. This reduces a dataset into just fractal parameter and some residual , that can be used to computed to regain the data. 
